name: Backend CI Pipeline

on:
  push:
    branches:
      - "feat/**"
      - "fix/**"
      - "chore/**"
      - "refactor/**"
  pull_request:
    branches:
      - "dev"

jobs:
  Backend_lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Setup Pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Nodejs
        uses: actions/setup-node@v5
        with:
          node-version: "22.x"
          cache: "pnpm"
          cache-dependency-path: "**/pnpm-lock.yaml"

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: install dependencies
        working-directory: apps/rezzident_BE
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: lint/format with ruff, flake8 & black
        run: |
          ruff check . && black --check .
          ruff check --fix . && black .
          mypy api/ --ignore-missing-imports

  Backend_test:
    needs:
      - Backend_lint
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Setup Pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Nodejs
        uses: actions/setup-node@v5
        with:
          node-version: "22.x"
          cache: "pnpm"
          cache-dependency-path: "**/pnpm-lock.yaml"

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: install dependencies
        working-directory: apps/rezzident_BE
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test with pytest
        run: pnpm turbo run test --filter=rezzident-be
